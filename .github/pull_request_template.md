## Summary
<!-- What does this PR do? One line. -->

## Type
- [ ] `feat:` — new feature (semver minor bump)
- [ ] `fix:` — bug fix (semver patch bump)
- [ ] `feat!:` — breaking change (semver major bump)
- [ ] `chore:` / `docs:` — no version bump

## Changes
<!-- List files/sections changed -->

## Testing
- [ ] `make validate` passes locally
- [ ] ESPHome compile tested (`make compile`) if touching C++ or I2S config
- [ ] Secrets NOT included in diff (`make secrets-check` passes)

## HA Entities Affected
<!-- Any new or renamed entities that break existing HA automations? -->
None
